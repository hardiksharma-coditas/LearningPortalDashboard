import { PurchaseButtonPermit } from './purchase-button-permit';

describe('PurchaseButtonPermit', () => {
  it('should create an instance', () => {
    const directive = new PurchaseButtonPermit();
    expect(directive).toBeTruthy();
  });
});
