import { EditDeletepermitAdmin } from './edit-deletepermit-admin';

describe('EditDeletepermitAdmin', () => {
  it('should create an instance', () => {
    const directive = new EditDeletepermitAdmin();
    expect(directive).toBeTruthy();
  });
});
