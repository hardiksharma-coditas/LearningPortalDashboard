import { EditDeleteCoursePermit } from './edit-delete-course-permit';

describe('EditDeleteCoursePermit', () => {
  it('should create an instance', () => {
    const directive = new EditDeleteCoursePermit();
    expect(directive).toBeTruthy();
  });
});
